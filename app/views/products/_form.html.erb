<center>
<%= form_with(model: product) do |f| %>
  <% if product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
        <% product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.text_field  :title, style: "display: block", class: "form-control input_new_product",placeholder: "Title" %>
 

    <%= f.text_area :description, class: "form-control input_new_product",placeholder: "Description" , rows: 7%>
    <%= f.text_field  :image_url, class: "form-control input_new_product",placeholder: "Image" %>
    <%= f.text_field :price, class: "form-control input_new_product",placeholder: "Price" %>
    <br>
    <%= link_to "Back to products", products_path , class: "btn btn-primary"%> <%= f.submit "Publish", class: "btn btn-primary", style: "margin-left: 115px" %>
  </div>
<% end %>
</center>